return {

    -- No `name = "plenary"` here: every other spec depends on it as
    -- "nvim-lua/plenary.nvim", and the alias made lazy treat them as two separate
    -- plugins and clone the repo twice.
    "nvim-lua/plenary.nvim",

    "eandrju/cellular-automaton.nvim",
}
